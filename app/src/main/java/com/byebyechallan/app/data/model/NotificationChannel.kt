package com.byebyechallan.app.data.model

/**
 * Notification channels accepted by the backend (Map keys on ProfileRequestDto.recipients).
 */
enum class NotificationChannel {
    EMAIL,
    WHATSAPP,
    SMS
}
